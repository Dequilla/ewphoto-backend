{
  "routes": [
    {
      "method": "GET",
      "path": "/products",
      "handler": "product.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/products/count",
      "handler": "product.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/products/:slug",
      "handler": "product.findOne",
      "config": {
        "policies": []
      }
    }
  ]
}
